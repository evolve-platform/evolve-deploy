/evolve-deploy
/dist/
coverage.txt
.DS_Store

# The first spec: internal as it stands, because it names an AWS account id and
# an artifact bucket. Scrub those and it can come out of here.
specs/initial.md
